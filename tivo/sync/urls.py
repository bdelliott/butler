from django.conf.urls import patterns, url

urlpatterns = patterns('',
        # No URLs at this time.  Sync jobs are handled through a management
        # command.  The library app exposes the 'now playing' list.
)
